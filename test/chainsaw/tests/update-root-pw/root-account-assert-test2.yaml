apiVersion: mariadb.openstack.org/v1beta1
kind: MariaDBAccount
metadata:
  name: openstack-mariadb-root
spec:
  userName: root
  secret: special-char-test2-secret
  accountType: System
status:
  # Verify that the CurrentSecret field has been updated to the new secret
  currentSecret: special-char-test2-secret
